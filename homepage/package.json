{
  "name": "zeigmers",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build:grafik": "node tools/build_landing_svg.mjs",
    "prebuild": "npm run build:grafik",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "build:data": "uv run --project etl zeigmers-etl all"
  },
  "dependencies": {
    "@deck.gl/core": "^9.0.0",
    "@deck.gl/layers": "^9.0.0",
    "@deck.gl/mapbox": "^9.0.0",
    "maplibre-gl": "^4.7.1"
  },
  "devDependencies": {
    "jsdom": "^30.0.1",
    "mapshaper": "^0.6.102",
    "typescript": "^5.6.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  }
}
